---
phase: 01-foundation-data-ingestion-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/001_schema.sql
  - supabase/migrations/002_rls.sql
  - supabase/migrations/003_indexes.sql
  - supabase/README.md
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
user_setup:
  - "Create a project in Supabase Dashboard and retrieve SUPABASE_URL and SUPABASE_SERVICE_KEY."

must_haves:
  truths:
    - "Supabase PostgreSQL schema defines all tables with relational constraints matching the architecture ERD."
    - "Row-Level Security allows public anonymous read of catalog items while locking user profiles, preferences, and favorites to auth.uid()."
    - "GIN indexes exist on text arrays (allergens, dietary_tags) and trigram indexes exist on product and variant names."
    - "Storage bucket configuration is documented for product images."
  artifacts:
    - supabase/migrations/001_schema.sql
    - supabase/migrations/002_rls.sql
    - supabase/migrations/003_indexes.sql
    - supabase/README.md
  key_links:
    - "product_variants foreign key references products.id"
    - "variant_red_flags foreign key references product_variants.id"
    - "user_preferences foreign key references users.id"
---

<objective>
Deliver the complete Supabase PostgreSQL database architecture including schema tables, foreign key relationships, row-level security (RLS) policies, performance indexes, and storage bucket configuration.

Purpose: Provide the persistent data storage and security foundation required for both the Python ingestion pipeline (ETL) and the Next.js consumer web application.
Output: Modular SQL migration scripts (`001_schema.sql`, `002_rls.sql`, `003_indexes.sql`) and setup documentation.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-ingestion-pipeline/01-CONTEXT.md
@implementation_plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Database Schema Migration (001_schema.sql)</name>
  <files>supabase/migrations/001_schema.sql</files>
  <read_first>implementation_plan.md, .planning/phases/01-foundation-data-ingestion-pipeline/01-CONTEXT.md</read_first>
  <action>
    Create `supabase/migrations/001_schema.sql` implementing the complete relational schema described in Section 2 of implementation_plan.md:
    1. `brands`: `id uuid primary key default gen_random_uuid()`, `name text not null`, `slug text not null unique`, `logo_url text`, `website_url text`, `description text`, `is_verified boolean default false`, `created_at timestamptz default now()`.
    2. `categories`: `id uuid primary key default gen_random_uuid()`, `name text not null`, `slug text not null unique`, `description text`, `icon text`, `display_order int default 0`.
    3. `products`: `id uuid primary key default gen_random_uuid()`, `brand_id uuid references brands(id) on delete cascade`, `category_id uuid references categories(id) on delete restrict`, `name text not null`, `slug text not null unique`, `description text`, `image_url text`, `is_active boolean default true`, `created_at timestamptz default now()`, `updated_at timestamptz default now()`.
    4. `product_variants`: `id uuid primary key default gen_random_uuid()`, `product_id uuid references products(id) on delete cascade`, `variant_name text not null`, `slug text not null unique`, `sku text`, `barcode_ean text`, `mrp_inr numeric(10,2) not null`, `net_weight_g numeric(10,2) not null`, `serving_size_g numeric(10,2) not null`, `servings_per_pack int`, `serving_size_label text`, `calories_kcal numeric(10,2) not null`, `protein_g numeric(10,2) not null`, `total_fat_g numeric(10,2) not null`, `saturated_fat_g numeric(10,2) default 0`, `trans_fat_g numeric(10,2) default 0`, `cholesterol_mg numeric(10,2) default 0`, `total_carbs_g numeric(10,2) not null`, `dietary_fiber_g numeric(10,2) default 0`, `total_sugars_g numeric(10,2) default 0`, `added_sugars_g numeric(10,2) default 0`, `sodium_mg numeric(10,2) default 0`, `additional_nutrients jsonb default '{}'`, `ingredient_list text[] not null default '{}'`, `ingredient_deck_raw jsonb not null default '[]'`, `allergens text[] not null default '{}'`, `dietary_tags text[] not null default '{}'`, `primary_protein_source text`, `protein_tier text`, `has_added_free_form_aminos boolean default false`, `cost_per_g_protein numeric(10,2)`, `protein_density_pct numeric(5,2)`, `true_net_carbs_g numeric(10,2)`, `image_url text`, `is_active boolean default true`, `last_verified_at timestamptz default now()`, `created_at timestamptz default now()`, `updated_at timestamptz default now()`.
    5. `variant_red_flags`: `id uuid primary key default gen_random_uuid()`, `variant_id uuid references product_variants(id) on delete cascade`, `flag_type text not null`, `flag_severity text not null check (flag_severity in ('high', 'amber', 'info'))`, `flag_label text not null`, `flag_description text not null`, `matched_ingredient text`, `ins_number text`, `created_at timestamptz default now()`.
    6. `redirect_links`: `id uuid primary key default gen_random_uuid()`, `variant_id uuid references product_variants(id) on delete cascade`, `platform text not null check (platform in ('amazon', 'blinkit', 'zepto', 'instamart', 'd2c'))`, `url text not null`, `platform_price_inr numeric(10,2)`, `price_last_checked timestamptz default now()`, `created_at timestamptz default now()`.
    7. `users`: `id uuid primary key references auth.users(id) on delete cascade`, `email text`, `display_name text`, `avatar_url text`, `created_at timestamptz default now()`.
    8. `user_preferences`: `id uuid primary key default gen_random_uuid()`, `user_id uuid unique references users(id) on delete cascade`, `daily_protein_target_g numeric(10,2)`, `body_weight_kg numeric(10,2)`, `dietary_restrictions text[] default '{}'`, `allergens text[] default '{}'`, `avoided_ingredients text[] default '{}'`, `preferred_protein_tier text`, `max_budget_per_g numeric(10,2)`.
    9. `user_favorites`: `id uuid primary key default gen_random_uuid()`, `user_id uuid references users(id) on delete cascade`, `variant_id uuid references product_variants(id) on delete cascade`, `created_at timestamptz default now()`, `unique (user_id, variant_id)`.
    Include updated_at triggers and standard uuid generation.
  </action>
  <verify>test -f supabase/migrations/001_schema.sql</verify>
  <acceptance_criteria>
    - supabase/migrations/001_schema.sql contains `create table if not exists brands`
    - supabase/migrations/001_schema.sql contains `create table if not exists product_variants`
    - supabase/migrations/001_schema.sql contains `create table if not exists variant_red_flags`
    - supabase/migrations/001_schema.sql contains `create table if not exists user_preferences`
    - supabase/migrations/001_schema.sql contains `ingredient_list text[]`
  </acceptance_criteria>
  <done>Core schema SQL defines all 9 tables with proper types, foreign keys, and constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Row-Level Security Policies (002_rls.sql)</name>
  <files>supabase/migrations/002_rls.sql</files>
  <read_first>supabase/migrations/001_schema.sql, implementation_plan.md</read_first>
  <action>
    Create `supabase/migrations/002_rls.sql` enabling RLS and defining access policies:
    1. Enable RLS on all 9 tables (`alter table brands enable row level security`, etc.).
    2. Catalog tables (`brands`, `categories`, `products`, `product_variants`, `variant_red_flags`, `redirect_links`):
       - Create policy for public read: `for select using (is_active = true)` or `for select using (true)`.
       - Restrict insert/update/delete to service role (`to service_role using (true) with check (true)`).
    3. User profile and preference tables (`users`, `user_preferences`, `user_favorites`):
       - `users`: select/update `using (auth.uid() = id)`.
       - `user_preferences`: select/insert/update/delete `using (auth.uid() = user_id) with check (auth.uid() = user_id)`.
       - `user_favorites`: select/insert/delete `using (auth.uid() = user_id) with check (auth.uid() = user_id)`.
    4. Include automatic profile creation trigger on `auth.users` insert:
       `create or replace function public.handle_new_user()` inserting into `public.users(id, email, display_name, avatar_url)`.
  </action>
  <verify>test -f supabase/migrations/002_rls.sql</verify>
  <acceptance_criteria>
    - supabase/migrations/002_rls.sql contains `alter table product_variants enable row level security`
    - supabase/migrations/002_rls.sql contains `auth.uid() = user_id`
    - supabase/migrations/002_rls.sql contains `public.handle_new_user()`
  </acceptance_criteria>
  <done>RLS policies protect user records while keeping catalog tables publicly readable.</done>
</task>

<task type="auto">
  <name>Task 3: Performance, Trigram & Array Indexes (003_indexes.sql)</name>
  <files>supabase/migrations/003_indexes.sql</files>
  <read_first>supabase/migrations/001_schema.sql, implementation_plan.md</read_first>
  <action>
    Create `supabase/migrations/003_indexes.sql`:
    1. Enable `pg_trgm` extension (`create extension if not exists pg_trgm;`).
    2. B-Tree indexes for sorting & joins:
       - `idx_variants_cost_per_g on product_variants (cost_per_g_protein) where is_active = true`
       - `idx_variants_protein_density on product_variants (protein_density_pct desc) where is_active = true`
       - `idx_variants_protein_tier on product_variants (protein_tier) where is_active = true`
       - `idx_variants_product_id on product_variants (product_id)`
       - `idx_products_category on products (category_id) where is_active = true`
       - `idx_products_brand on products (brand_id) where is_active = true`
       - `idx_red_flags_variant on variant_red_flags (variant_id)`
       - `idx_redirect_platform on redirect_links (variant_id, platform)`
    3. GIN indexes for array filtering (required for recommendation engine Step 1 & 2):
       - `idx_variants_allergens on product_variants using gin (allergens)`
       - `idx_variants_dietary_tags on product_variants using gin (dietary_tags)`
    4. Trigram fuzzy search indexes:
       - `idx_products_name_trgm on products using gin (name gin_trgm_ops)`
       - `idx_variants_name_trgm on product_variants using gin (variant_name gin_trgm_ops)`
  </action>
  <verify>test -f supabase/migrations/003_indexes.sql</verify>
  <acceptance_criteria>
    - supabase/migrations/003_indexes.sql contains `create extension if not exists pg_trgm`
    - supabase/migrations/003_indexes.sql contains `idx_variants_allergens on product_variants using gin (allergens)`
    - supabase/migrations/003_indexes.sql contains `idx_products_name_trgm on products using gin (name gin_trgm_ops)`
  </acceptance_criteria>
  <done>All B-Tree, GIN array, and pg_trgm search indexes are specified.</done>
</task>

<task type="auto">
  <name>Task 4: Storage Bucket Configuration and Documentation (supabase/README.md)</name>
  <files>supabase/README.md</files>
  <read_first>supabase/migrations/001_schema.sql, supabase/migrations/002_rls.sql, supabase/migrations/003_indexes.sql</read_first>
  <action>
    Create `supabase/README.md` containing:
    1. Overview of the database topology and entity relationships.
    2. Step-by-step instructions for running migrations:
       - Option A: Via Supabase CLI (`supabase db push` / `supabase migration up`).
       - Option B: Via Supabase Web Dashboard SQL Editor (execute `001_schema.sql`, `002_rls.sql`, `003_indexes.sql` in sequence).
    3. Storage bucket setup instructions for `product-images`:
       - Bucket name: `product-images` (Public read enabled).
       - Policy: Public read (`anon` role), insert/update restricted to authenticated admins or service role.
    4. Supabase Auth configuration guidelines for Google OAuth provider (Client ID, Client Secret, callback URL).
    5. Environment variables template reference (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`).
  </action>
  <verify>test -f supabase/README.md</verify>
  <acceptance_criteria>
    - supabase/README.md contains `product-images`
    - supabase/README.md contains `001_schema.sql`
    - supabase/README.md contains `Google OAuth`
  </acceptance_criteria>
  <done>Supabase setup and migration guide is fully documented.</done>
</task>

</tasks>

<artifacts_produced>
- supabase/migrations/001_schema.sql
- supabase/migrations/002_rls.sql
- supabase/migrations/003_indexes.sql
- supabase/README.md
</artifacts_produced>
